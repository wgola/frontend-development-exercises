import { createTheme } from "@mui/material/styles";

export default createTheme({
  palette: {
    background: {
      default: "#565551",
      main: "#a3a198",
    },
    primary: {
      main: "#f3eadb",
    },
    secondary: {
      main: "#e4f3db",
    },
    success: {
      main: "#dbf3de",
    },
    error: {
      main: "#000000",
    },
  },
});
