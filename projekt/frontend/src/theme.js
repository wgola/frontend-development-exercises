import { createTheme } from "@mui/material/styles";

export default createTheme({
  palette: {
    background: {
      default: "#f0f3db",
      main: "#dedbf3",
    },
    primary: {
      // main: "#000000",
      main: "#f3eadb",
    },
    secondary: {
      main: "#e4f3db",
      // main: "#000000",
    },
    success: {
      main: "#dbf3de",
    },
    error: {
      main: "#000000",
    },
  },
});
